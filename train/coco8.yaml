# Watermark Detection Dataset - COCO8 format
# Train/val/test sets

# Dataset root (absolute path avoids cache collisions)
path: C:/Users/waxpl/Desktop/SoraWMR/SoraWatermarkCleaner/datasets/coco8

# Train/val/test splits (relative to path). Add extra folders as needed.
train:
  - images/train_manual
  - images/train_aug
val:
  - images/train_manual
test: []

# Class names (order must match label files)
names:
  0: watermark
  1: text
